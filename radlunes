import matplotlib.pyplot as plt
import numpy as np

#r=a+acos()
#esta grafica se llama cardioide


plt.axes(projection='polar')
#a=4
#rads=np.arange(0,(2*np.pi),0.01)
#for rad in rads:

#    r=a*np.cos(rad*10)#+(a*np.cos(rad))
 #   plt.plot(rad,r,'g.')

#plt.show()

#INCISO A PROB2
a=4

rads=np.arange(0,4*np.pi,0.01)
for rad in rads:

    r=np.cos(2*rad)+np.sin(3*rad)
    r2=-2*np.sin(2*rad)+3*np.cos(3*rad)
    plt.plot(rad,r,'g.')

plt.show()
